# Testing system

[![tests](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/FormingWorlds/Zalmoxis/main/.github/badges/tests-total.json)](https://proteus-framework.org/testing)
[![unit tests](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/FormingWorlds/Zalmoxis/main/.github/badges/tests-unit.json)](https://proteus-framework.org/testing)
[![integration tests](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/FormingWorlds/Zalmoxis/main/.github/badges/tests-integration.json)](https://proteus-framework.org/testing)
[![codecov](https://codecov.io/gh/FormingWorlds/Zalmoxis/graph/badge.svg)](https://codecov.io/gh/FormingWorlds/Zalmoxis)
![CI](https://github.com/FormingWorlds/Zalmoxis/actions/workflows/CI.yml/badge.svg)

## What testing here means

Tests verify that the code does what was written; physical correctness is judged by data, not by tests. A test passing tells the developer that the implementation matches the design intent (the Picard loop converges, the binodal sigmoid is symmetric, the helpfile schema has the expected columns); it does not tell anyone that the planet model is right. That judgement comes from comparing model output against published data and observations, which is a separate workflow.

## Internal 4-marker scheme

Every test in the suite carries exactly one pytest marker:

- `unit`: mocked physics, fast (target < 100 ms each).
- `smoke`: real binaries on minimal configurations (target < 30 s each).
- `integration`: real binaries with full coupling.
- `slow`: anything > 1 minute.

## Running tests locally

```console
pytest -m unit
pytest -m smoke
pytest -m integration
pytest -m slow
pytest -m "not skip"   # full suite
```

## Public 2-category scheme

Public-facing badges (README, website) collapse `smoke` + `integration` + `slow` into a single "Integration Tests" category, because a 4-way taxonomy is confusing to non-developer readers. The 4-marker internal scheme remains for CI infrastructure granularity (different timeouts, different schedules, push vs nightly tier separation).

## Badge system

Three shields.io endpoint-badge JSON files are kept in `.github/badges/`:

- `tests-total.json`: count of all tests excluding `skip`.
- `tests-unit.json`: count of `@pytest.mark.unit` tests.
- `tests-integration.json`: combined count of `@pytest.mark.smoke`, `@pytest.mark.integration`, and `@pytest.mark.slow` tests.

The files are regenerated by the `Refresh test count badges` GitHub Actions workflow on every push to `main` whose paths touch `tests/`, `src/`, `pyproject.toml`, the workflow YAML, or `tools/generate_test_badges.py`. Shields.io fetches them via the raw GitHub URL and renders the badge live.

## Canonical specification

The ecosystem-wide testing standard, of which the 2-category public scheme and the 4-marker internal scheme are part, is documented at [proteus-framework.org/PROTEUS/Explanations/ecosystem_testing_standard/](https://proteus-framework.org/PROTEUS/Explanations/ecosystem_testing_standard/).
